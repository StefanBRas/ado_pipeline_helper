# some comment here

trigger:
- branch_that_does_not_exist 

name: test2extends

extends:
  template: templates/stages/complex.yml
  parameters:
    jobs:  
      - bash: echo Test 
        displayName: succeed
      - bash: echo "Test"
        displayName: succeed

