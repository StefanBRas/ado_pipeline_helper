# some comment here

trigger:
- branch_that_does_not_exist 

name: test2extends

extends:
  template: templates/stages/complex.yml
  parameters:
    buildSteps:  
      - bash: echo Test 
        displayName: succeed
      - bash: echo "Test"
        displayName: succeed

